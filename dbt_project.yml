name: 'dbt_project'
version: '1.0'
config-version: 2

profile: 'default'

model-paths: ['models']
target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_modules'

models:
  dbt_project:
    staging:
      materialized: view
    marts:
      materialized: table
